[pytest]
# langsmith_plugin pulls uuid_utils, whose native DLL is blocked by Windows
# Application Control on this machine; disable it to keep the standard
# `python -m pytest policy/tests tools/tests` command working.
addopts = -p no:langsmith_plugin
testpaths = policy/tests tools/tests
